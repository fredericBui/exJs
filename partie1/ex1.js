// Les variables permettent de stocker des valeurs pour pouvoir les réutiliser plus tard

// Variable de type texte, guillemets obligatoire
var prenom = "John"; 
var nom = "Doe";

// Variable de type chiffre
var age = 42; 

// Alert affiche les valeurs contenues dans les variables prenom, nom et age
alert(prenom + " " + nom + " " + age);

// Défi : Modifier les valeurs des variables